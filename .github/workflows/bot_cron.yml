name: Fixed 1-Hour OmniSync Bot

on:
  workflow_dispatch:
  repository_dispatch:
    types: [run-sync]
  schedule:
    - cron: '0 * * * *'

permissions:
  contents: write

jobs:
  run-sync-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Setup Python environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies, Fonts & Rclone
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg libraqm-dev fonts-beng rclone

      - name: Install Python Requirements
        run: |
          pip install -r requirements.txt

      - name: Execute OmniSync Master Engine
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_SESSION: ${{ secrets.TG_SESSION }}
          FB_TOKEN: ${{ secrets.FB_TOKEN }}
          RENDER_WA_URL: ${{ secrets.RENDER_WA_URL }}
          YT_CLIENT_ID: ${{ secrets.CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
          YT_CLIENT_ID_2: ${{ secrets.CLIENT_ID_2 }}
          YT_CLIENT_SECRET_2: ${{ secrets.CLIENT_SECRET_2 }}
          YT_REFRESH_TOKEN_2: ${{ secrets.REFRESH_TOKEN_2 }}
          BUFFER_PROFILE_ID: ${{ secrets.BUFFER_PROFILE_ID }}
          BUFFER_ACCESS_TOKEN: ${{ secrets.BUFFER_ACCESS_TOKEN }}
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
          OLLAMA_API_URL: ${{ secrets.OLLAMA_API_URL }}
          GROQ_API: ${{ secrets.GROQ_API }}
          ELEVENLABS_API_KEYS: ${{ secrets.ELEVENLABS_API_KEYS }}
          
          # 🌟 Rclone & Google Drive
          RCLONE_CONF: ${{ secrets.RCLONE_CONF }}
          GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
          SAVE_TO_GDRIVE: ${{ secrets.SAVE_TO_GDRIVE }}
          ENABLE_TIKTOK: ${{ secrets.ENABLE_TIKTOK }}
          USE_LOCAL_MUSIC: ${{ secrets.USE_LOCAL_MUSIC }}
        run: python run_on_github.py

      - name: Commit & Save Sync Progress State
        run: |
          git config --local user.email "engine@omnisync.io"
          git config --local user.name "OmniSync Master Engine"
          git add bot_memory.json
          git commit -m "Auto-Update: Visual & Multi-Output checkpoint [skip-ci]" || echo "No new updates to save"
          git push
